<h1>Cards:</h1>

<section>
      <p style="font-size: 28px;">
      <li> <%= @card.original_text  %></li>
      <li> <%= @card.translated_text %></li>
      <li> <%= @card.review_date %></li>
      <li> <%= image_tag @card.image.thumb.url if @card.image? %></li>
      </p>
</section>

 <%= link_to 'Edit', edit_card_path(@card) %> 
 <%= link_to 'Back', cards_path %>

