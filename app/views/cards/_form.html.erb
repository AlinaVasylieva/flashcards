<%= simple_form_for current_user.cards do |f| %>
  <% if current_user.cards.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(current_user.cards.errors.count, "error") %> prohibited
        this card from being saved:
      </h2>
      <ul>
        <% current_user.cards.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= f.label :original_text %><br>
    <%= f.text_field :original_text %>
  </p>
 
  <p>
    <%= f.label :translated_text %><br>
    <%= f.text_area :translated_text %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
